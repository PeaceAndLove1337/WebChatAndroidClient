<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.webchatclient"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WebChatClient">
        <activity
            android:name=".ui.presentation.activity.ChooseChatActivity"
            android:exported="true" />
        <activity
            android:name=".ui.presentation.activity.ChatActivity"
            android:exported="true" />
        <activity
            android:name=".ui.presentation.activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>